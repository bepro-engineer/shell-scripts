# shell-scripts

## このリポジトリの目的

Shellスクリプト群の作成・整理・見直しを行う。

## 方針

- 修正は必ず関数単位で行う
- 既存コードの全面書き換えはしない
- まず現状整理、次に問題点抽出、その後に修正を行う
- 対象は都度指定された com 配下のシェルスクリプトとする

## Shell Script Rules

### Scope

このリポジトリ内の Shell Script 修正・追加・レビューでは、以下の規約を必ず優先すること。

### Basic Policy

- 修正は必ず関数単位で行う
- 既存コードの全面書き換えはしない
- まず現状整理、次に問題点抽出、その後に修正を行う
- 指示されていないディレクトリ名・ファイル名・構造を勝手に新規作成しない
- 不明点は推測せず停止する
- 既存の関数名・変数名・責務を勝手に再設計しない
- 変更対象外の関数には触れない

### Naming Rules

- 関数名は必ずキャメルケースを使用する
- 変数名はスネークケースを使用する
- グローバル変数は大文字スネークケースを使用する
- ローカル変数は `local` を付け、スネークケースを使用する
- 真偽値判定の関数名は `is...`
- 取得系の関数名は `get...`
- 実行系で動詞以外を扱う場合のみ `exec...` を使用する

### File Naming Rules

- 実行ファイル系 Shell Script の拡張子は `.sh`
- 共通関数・共通処理などのリソース系 Shell Script の拡張子は `.shrc`

### Directory Rules

Shell の前提ディレクトリ構成は以下とする。

- `projects/scripts/bin`
- `projects/scripts/com`
- `projects/scripts/etc`
- `projects/scripts/log`
- `projects/scripts/rep`
- `projects/scripts/tmp`

共通スクリプトは以下を前提とする。

- ログ出力: `projects/scripts/com/logger.shrc`
- 共通処理: `projects/scripts/com/utils.shrc`

### Logging Rules

- ログ出力は必ず `logOut` を使用する
- `logInfo` や `logError` などの独自略称関数は使用しない
- 形式は必ず以下とする

```bash
logOut "INFO" "message"
logOut "WARNING" "message"
logOut "ERROR" "message"
```

### Function Comment Rules

すべての関数には必ずコメントを付けること。
コメント形式は必ず以下のテンプレートを使うこと。

```テキスト
# ------------------------------------------------------------------
# 関数名　　：xxx
# 概要　　　：○○○
# 説明　　　：
# ○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○
# ○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○○
#
# 引数　　　：説明
# 戻り値　　：説明
# 使用箇所　：説明
# ------------------------------------------------------------------
```

### Implementation Rules

- Bash 依存を増やさない
- `echo -e` より `printf` を優先する
- グローバル変数への隠れ依存を避け、必要な値は引数で受け渡す
- 冗長な `return $?` は書かない
- 未使用関数は残さず、呼び出し有無を確認して整理する
- 責務が重複している関数は、共通処理へ寄せて整理する
- 文字列比較、数値比較、空判定は境界ケースを意識する
- `set -u` 環境で落ちないように引数不足を考慮する

### Edit Rules

- 修正対象ファイルだけを変更する
- 差分は最小にする
- コメントだけ直す場合も、実装との整合が取れていること
- 既存セクション名・関数名を勝手に変えない

### Validation Rules

修正後は必ず以下を行うこと。

1. 構文確認  
   `bash -n 対象ファイル`

2. 差分確認  
   `git diff -- 対象ファイル`

3. 必要最小限の動作確認  
   対象関数を source して直接実行し、戻り値または出力を確認する

### Git Rules

- cc-tasks の開始から終了までの固定手順に従う
- 作業前に task を start する
- 作業は feature ブランチで行う
- 対象ファイルだけ add する
- main へ merge 後、push と branch 削除まで行う
