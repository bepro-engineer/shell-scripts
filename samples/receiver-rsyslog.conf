# /etc/rsyslog.d/10-relp-receive-20514.conf
# Receive RELP logs on port 20514 and store application logs by sender hostname.

module(load="imrelp")
input(type="imrelp" port="20514")

$template RemoteAppLog,"/var/log/remote/%HOSTNAME%/app.log"

local5.* ?RemoteAppLog
& stop
